# .env.secret — runtime secrets injected into the dev container (arch-09 §5).
#
# THIS IS A TEMPLATE. The real file is GENERATED by scripts/init-workspace.sh
# from your host environment and MUST be git-ignored. Never commit real values.
#
# Add ".env.secret" to .gitignore before running init-workspace.sh.

ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
GEMINI_API_KEY=
GH_TOKEN=
